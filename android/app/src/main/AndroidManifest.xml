<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Allow app to check if any other app can handle URLs -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
    </queries>

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.any" android:required="false" />

    <!-- Runtime-prompted (API 33+) for the "notify on sweep complete"
         setting; see SweepNotificationService. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- For the idle memory sweep's Android background path: a periodic
         WorkManager job starts a foreground service to load a model and
         run inference with the app fully closed. See
         android/app/src/main/java/com/pocketpalai/memorysweep/. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <!-- Needed only for devices running Android 9 (API 28) or lower -->
    <!-- Currently used for storing chat sessions (/Download) -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="true"
      android:dataExtractionRules="@xml/backup_rules_12_plus"
      android:fullBackupContent="@xml/backup_rules_legacy"
      android:networkSecurityConfig="@xml/network_security_config"
      android:theme="@style/AppTheme"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode|density"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        <!-- Host-scoped deep link for the Hugging Face "Use this model" flow.
             Scoped to host=hub so this is not a bare-scheme handler. -->
        <intent-filter>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.DEFAULT" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="pocketpal" android:host="hub" />
        </intent-filter>
        <!-- Checkout callback for the in-app PalsHub purchase flow. Scoped to
             host=checkout so it is routed to AuthSessionModule, not the
             hub deep-link path. -->
        <intent-filter>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.DEFAULT" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="pocketpal" android:host="checkout" />
        </intent-filter>
      </activity>

        <!-- Runs the idle memory sweep's headless JS task as a foreground
             service, started by MemorySweepWorker on WorkManager's
             schedule. dataSync is the closest fit among Android's declared
             foreground-service types for local, on-device reprocessing of
             the user's own data; there is no type specific to on-device
             AI inference. -->
        <service
            android:name=".memorysweep.MemorySweepHeadlessService"
            android:foregroundServiceType="dataSync"
            android:exported="false" />

        <!-- Optional native library for OpenCL (GPU acceleration) -->
        <uses-native-library
            android:name="libOpenCL.so"
            android:required="false" />

        <!-- Optional native library for Hexagon DSP (NPU acceleration) -->
        <uses-native-library
            android:name="libcdsprpc.so"
            android:required="false" />
    </application>

</manifest>
